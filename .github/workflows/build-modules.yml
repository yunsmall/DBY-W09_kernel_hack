name: Build Modules

on:
  workflow_dispatch:
    inputs:
      config_url:
        description: '内核 .config 文件下载地址（如 gist raw URL）'
        required: true
        type: string

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: Install toolchain
        run: |
          sudo apt-get update
          sudo apt-get install -y clang lld llvm gcc-aarch64-linux-gnu binutils-aarch64-linux-gnu

      - name: Download kernel config
        run: |
          curl -sSL "${{ inputs.config_url }}" -o /tmp/kernel_config
          if ! grep -q 'CONFIG_' /tmp/kernel_config; then
            echo "ERROR: not a valid kernel config file"
            exit 1
          fi
          cp /tmp/kernel_config analysis/my_tablet_origin_config

      - name: Setup kernel source
        run: python3 tools/setup_kernel_source.py

      - name: Build
        run: |
          . env.sh

          mkdir -p output/modules_build
          cp analysis/my_tablet_origin_config output/modules_build/.config
          dby-w09-4.0/scripts/config --file output/modules_build/.config --disable MODULE_SIG_ALL
          make -C dby-w09-4.0 O=../output/modules_build olddefconfig
          make -C dby-w09-4.0 O=../output/modules_build modules_prepare -j$(nproc)
          make -C dby-w09-4.0 O=../output/modules_build modules -j$(nproc)

          # SELinux module
          make -C dby-w09-4.0 O=../output/modules_build M=../selinux_module modules

          # Install all modules to staging dir.
          MODULES_DIR="$PWD/output/modules_install"
          mkdir -p "$MODULES_DIR"
          make -C dby-w09-4.0 O=../output/modules_build INSTALL_MOD_PATH="$MODULES_DIR" modules_install || true

      - name: Package
        run: |
          # All modules (installed tree, excludes SELinux)
          tar czf output/all-modules.tar.gz -C output/modules_install .
          # SELinux module
          cp output/selinux_module/selinux_permissive.ko output/

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: all-modules
          path: output/all-modules.tar.gz

      - name: Upload SELinux
        uses: actions/upload-artifact@v4
        with:
          name: selinux-module
          path: output/selinux_permissive.ko
